pipeline {
        agent{
          kubernetes {
      label 'cassandra'
      defaultContainer 'jnlp'
      yaml '''
        apiVersion: v1
        kind: Pod
        metadata:
          labels:
            jenkins: label
        spec:
          containers:
          - name: alpine
            image: alpine:3.11
            command:
            - cat
            tty: true
        '''
    }
}

        stages {  
            stage ("first") {
                tools {
                   jdk "jdk_11_latest"
                }
                steps {
                    sh '$JAVA_HOME_11 -version'
                }
            }
            stage("second"){
                tools {
                   jdk "jdk_11_latest"
                }
                steps{
                    sh '$JAVA_HOME_17 -version'
                }
            }
       }
}
